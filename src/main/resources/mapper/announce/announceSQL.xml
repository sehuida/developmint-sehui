<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="announce">
	<select id="selectAllAnnounce" parameterType="_int" resultType="a">
		select
			announce_no as announceNo,
			member_no as memberNo,
			company_name as companyName,
			category,
			fields,
			school,
			work_form as workForm,
			announce_title as announceTitle,
			announce_content as announceContent,
			skills,
			money,
			work_place as workPlace,
			end_date as endDate,
			veterans,
			military,
			write_date as writeDate
		from announcement
		where member_no = #{memberNo}
	</select>
	
	<insert id="insertAnnounce" parameterType="a">
		insert into announcement values(announce_seq.nextval, #{memberNo}, #{companyName}, #{category}, #{fields}, #{school}, #{workForm}
		, #{announceTitle}, #{announceContent}, #{skills}, #{money}, #{workPlace}, #{endDate}, #{veterans}, #{military}, to_char(sysdate,'yyyy-mm-dd'))
	</insert>
	
	<select id="selectAllAnnounce" resultType="a">
		select
			announce_no as announceNo,
			member_no as memberNo,
			company_name as companyName,
			category,
			fields,
			school,
			work_form as workForm,
			announce_title as announceTitle,
			announce_content as announceContent,
			skills,
			money,
			work_place as workPlace,
			end_date as endDate,
			veterans,
			military,
			write_date as writeDate
		from announcement
	</select>
	
</mapper>
